info:
  name: Get Fields
  type: http
  seq: 6

http:
  method: GET
  url: "{{BaseAddress}}/rest/api/3/field"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
