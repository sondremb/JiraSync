info:
  name: Get Cloud Id
  type: http
  seq: 1

http:
  method: GET
  url: https://{{?Site name}}.atlassian.net/_edge/tenant_info
  params:
    - name: Site name}}.atlassian.net/_edge/tenant_info
      value: ""
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
