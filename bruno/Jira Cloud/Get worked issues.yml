info:
  name: Get worked issues
  type: http
  seq: 7

http:
  method: GET
  url: "{{BaseAddress}}/rest/api/3/search/jql?jql=worklogDate >= startOfWeek(-1) AND worklogDate <= endOfWeek(-1) AND worklogAuthor = currentUser()"
  params:
    - name: jql
      value: worklogDate >= startOfWeek(-1) AND worklogDate <= endOfWeek(-1) AND worklogAuthor = currentUser()
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
