info:
  name: Get Project Properties
  type: http
  seq: 5

http:
  method: GET
  url: "{{BaseAddress}}/rest/api/3/project/:key/properties"
  params:
    - name: key
      value: PASP
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
