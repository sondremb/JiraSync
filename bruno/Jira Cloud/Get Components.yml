info:
  name: Get Components
  type: http
  seq: 8

http:
  method: GET
  url: "{{BaseAddress}}/rest/api/3/component?projectIdsOrKeys=PASP"
  params:
    - name: projectIdsOrKeys
      value: PASP
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
