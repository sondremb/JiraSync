info:
  name: Get Issue
  type: http
  seq: 4

http:
  method: GET
  url: "{{BaseAddress}}/rest/api/3/issue/:issueIdOrKey"
  params:
    - name: issueIdOrKey
      value: PASP-5074
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
